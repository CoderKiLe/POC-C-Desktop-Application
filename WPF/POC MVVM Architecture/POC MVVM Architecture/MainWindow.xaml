<Window x:Class="POC_MVVM_Architecture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POC_MVVM_Architecture"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add" 
                    Command="{Binding AddCommand}"
                    Margin="0 0 5 0" 
                    Height="30" 
                    Padding="6 0"/>
            <Button Content="Save" 
                    Command="{Binding SaveItemButton}"
                    Margin="0 0 5 0" 
                    Height="30" 
                    Padding="6 0"/>
            <Button Content="Delete" 
                    Command="{Binding DeleteItemButton}"
                    Margin="0 0 5 0" 
                    Height="30" 
                    Padding="6 0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <DataGrid   
                CanUserAddRows="False" 
                CanUserDeleteRows="False"
                AutoGenerateColumns="False"
                ItemsSource="{Binding Items}"
                SelectedItem="{Binding SelectedItem}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="4*"/>
                    <DataGridTextColumn Header="SerialNumber" Binding="{Binding SerialNumber}" Width="4*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <Label Content="Name"></Label>
                <TextBox Margin="5 0 5 15" 
                         Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}">
                </TextBox>
                <Label Content="Serial Number"></Label>
                <TextBox Margin="5 0 5 15"
                         Text="{Binding SelectedItem.SerialNumber, UpdateSourceTrigger=PropertyChanged}">
                </TextBox>
                <Label Content="Quantity"></Label>
                <TextBox Margin="5 0 5 15"
                         Text="{Binding SelectedItem.Quantity, UpdateSourceTrigger=PropertyChanged}">
                </TextBox>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
